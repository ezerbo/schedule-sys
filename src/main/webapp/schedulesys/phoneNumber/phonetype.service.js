(function() {
	'use strict';
	
	angular
		.module('scheduleSys')
		.factory('PhoneTypeService', PhoneTypeService);
	
	PhoneTypeService.$Inject = ['$resource'];
	
	function PhoneTypeService($resource) {
		console.log('calling  phone type service');
		var resourceUrl = '/phone-number-types/:id';
		
		return $resource(resourceUrl, {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    if (data) {
                        data = angular.fromJson(data);
                    }
                    return data;
                }
            },
            'update': { method:'PUT' },
            'remove':  {
                method: 'DELETE',
                transformResponse: function (data) {
                    if (data) {
                        data = angular.toJson(data);
                    }
                    return data;
                }
            },
            'save': {
                method: 'POST',
                transformResponse: function (data) {
                    if (data) {
                        data = angular.toJson(data);
                    }
                    return data;
                }
            
            }
        });
		
	}
	
})();